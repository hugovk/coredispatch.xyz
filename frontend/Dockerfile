FROM oven/bun:1

WORKDIR /app

COPY package.json bun.lock ./
RUN bun install --frozen-lockfile

COPY . .

# Clean .next on every container start so stale cache never persists
EXPOSE 3000
CMD ["sh", "-c", "rm -rf .next && bun run dev"]
